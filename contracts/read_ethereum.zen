Rule caller restroom-mw

Scenario 'ethereum': retrieve data

Given I have a ethereum endpoint named 'evm_endpoint'
Given I read the ethereum bytes with hash 'txid' and save the output into 'retrieved_data'
Given I have a 'hex' named 'retrieved_data'

When I create the string from the ethereum bytes named 'retrieved_data'
When I rename the 'string' to 'data'

Then print the 'data'