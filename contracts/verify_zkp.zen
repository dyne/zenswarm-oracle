Scenario 'credential': verify proof

Given that I have a 'issuer public key'
and I have a 'credential proof'

When I aggregate all the issuer public keys
and I verify the credential proof

Then print the string 'The Proof was verified'